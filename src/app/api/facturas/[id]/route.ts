import { noImplementado } from "@/lib/api-stub";

export async function GET() {
  return noImplementado("GET /api/facturas/[id]");
}

export async function PATCH() {
  return noImplementado("PATCH /api/facturas/[id]");
}

export async function DELETE() {
  return noImplementado("DELETE /api/facturas/[id]");
}
